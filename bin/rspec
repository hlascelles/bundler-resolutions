#!/usr/bin/env ruby
# frozen_string_literal: true

#
# This file was generated by Bundler.
#
# The application 'rspec' is installed as part of a gem, and
# this file is here to facilitate running it.
#
bundle_binstub = File.expand_path("bundle", __dir__)

if File.file?(bundle_binstub)
  if File.read(bundle_binstub, 300).include?("This file was generated by Bundler")
    load(bundle_binstub)
  else
    abort("Your `bin/bundle` was not generated by Bundler, so this binstub cannot run.
Replace `bin/bundle` by running `bundle binstubs bundler --force`, then run this command again.")
  end
end

debug_on = ENV.fetch("BUNDLER_RESOLUTIONS_DEBUG", nil)
specs_running = ENV.fetch("BUNDLER_RESOLUTIONS_SPECS_RUNNING", nil)
ENV["BUNDLER_RESOLUTIONS_DEBUG"] = nil
ENV["BUNDLER_RESOLUTIONS_SPECS_RUNNING"] = nil

require "rubygems"
require "bundler/setup"

ENV["BUNDLER_RESOLUTIONS_DEBUG"] = debug_on
ENV["BUNDLER_RESOLUTIONS_SPECS_RUNNING"] = specs_running

load Gem.bin_path("rspec-core", "rspec")
